{
    "tasks": [
        {
            "label": "hello",
            "type": "shell",
            "command": "sleep 1s && echo ${env:USERNAME}",
            "problemMatcher": []
        },
        {
            "label": "Run",
            "type": "shell",
            "command": "clear && sleep 1s && gcc ${file} -o ${fileBasenameNoExtension} && ./${fileBasenameNoExtension}",
            "problemMatcher": [],
            "options": {
                "cwd": "${fileDirname}"
            },
            "presentation": {
                "reveal": "always",
                "focus": true,
                "panel": "dedicated",
                "showReuseMessage": false,
                "clear": true
            }
        }
    ]
}